package pos.pos;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.fail;

class CiBlockTest {

    @Test
    void thisTestShouldFail() {
        fail("CI block test — this is intentionally failing");
    }
}